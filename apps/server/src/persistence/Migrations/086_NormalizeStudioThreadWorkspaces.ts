// Historical tombstone. The exact migration ID and name are append-only lineage;
// Orchestrator Mode performs the real Studio purge in migration 088.
import { Effect } from "effect";

export default Effect.void;
